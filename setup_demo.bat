@echo off
rem Add path so commands can be easily executed

path %PATH%;%~dp0%x64\Release

echo Path to x64 release executables added
echo .
echo use zenfs commands to create and manage snapshots
echo .
echo zenfssvc runs the zenfs service
